{% extends 'base.html.twig' %}

{% block title %}
	{{ title|trans|capitalize }}
	-
	{{ parent() }}
{% endblock %}

{% block body %}
    <h1>Edit Tablette</h1>

    {{ include('tablette/_form.html.twig', {'button_label': 'Save'}) }}

    <a href="{{ path('tablette.list') }}">back to list</a>
{% if tablette.id %}
    {{ include('tablette/_delete_form.html.twig') }}
{% endif %}
{% endblock %}
